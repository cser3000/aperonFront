import styles from "./Footer.module.css"

const Footer = () => {
    return (
        <footer className={styles.footer}>
            <h3 className={styles.footerContent}>Апейрон все права защищены</h3>
        </footer>
    )
}

export default Footer;